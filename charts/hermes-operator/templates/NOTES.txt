hermes-operator has been deployed.

Operator namespace : {{ .Release.Namespace }}
Operator deployment: {{ include "hermes-operator.fullname" . }}

{{- if .Values.apiServer.enabled }}

REST API is enabled on port {{ .Values.apiServer.port }}.
Access it from within the cluster:

  curl http://{{ include "hermes-operator.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.apiServer.port }}/api/v1/hermesagents
{{- end }}

To deploy a HermesAgent, apply a CR:

  kubectl apply -f https://raw.githubusercontent.com/weiyuanke/hermes-operator/main/config/samples/core_v1_hermesagent.yaml

Check agent status:

  kubectl get hermesagents -A
