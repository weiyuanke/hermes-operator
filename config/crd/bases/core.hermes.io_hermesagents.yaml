---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hermesagents.core.hermes.io
spec:
  group: core.hermes.io
  names:
    kind: HermesAgent
    listKind: HermesAgentList
    plural: hermesagents
    singular: hermesagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: AI model
      jsonPath: .spec.model
      name: Model
      type: string
    - description: Model provider
      jsonPath: .spec.provider
      name: Provider
      type: string
    - description: Current phase
      jsonPath: .status.phase
      name: Status
      type: string
    - description: Gateway endpoint
      jsonPath: .status.gatewayEndpoint
      name: Gateway
      type: string
    - description: Dashboard endpoint
      jsonPath: .status.dashboardEndpoint
      name: Dashboard
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: HermesAgent is the Schema for the hermesagents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HermesAgent
            properties:
              apiSecretRef:
                description: |-
                  APISecretRef is the reference to a Kubernetes Secret containing the API key.
                  The secret should have a key named "api-key".
                properties:
                  key:
                    default: api-key
                    description: Key is the key in the Secret data. Defaults to "api-key".
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the Secret.
                      If not specified, the same namespace as the HermesAgent is used.
                    type: string
                required:
                - name
                type: object
              baseURL:
                default: https://api.moonshot.cn/v1
                description: |-
                  BaseURL is the API endpoint for the model provider.
                  Examples: "https://api.moonshot.cn/v1", "https://api.openai.com/v1"
                type: string
              dashboardPort:
                default: 9119
                description: DashboardPort is the port where the Hermes dashboard
                  service will listen.
                maximum: 65535
                minimum: 1
                type: integer
              dashboardResources:
                description: DashboardResources defines the compute resources for
                  the dashboard container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              gatewayPort:
                default: 8642
                description: GatewayPort is the port where the Hermes gateway service
                  will listen.
                maximum: 65535
                minimum: 1
                type: integer
              gatewayResources:
                description: GatewayResources defines the compute resources for the
                  gateway container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              image:
                default: docker.io/nousresearch/hermes-agent:latest
                description: Image is the container image for Hermes agent.
                type: string
              maxTurns:
                default: 90
                description: MaxTurns is the maximum number of conversation turns.
                type: integer
              model:
                description: |-
                  Model is the AI model name to use.
                  Examples: "kimi-k2.5", "gpt-4", "claude-3-sonnet"
                type: string
              personality:
                default: kawaii
                description: Personality is the agent's personality preset.
                type: string
              provider:
                description: |-
                  Provider is the model provider name.
                  Examples: "kimi-coding-cn", "openai", "openrouter"
                type: string
            required:
            - apiSecretRef
            - model
            - provider
            type: object
          status:
            description: status defines the observed state of HermesAgent
            properties:
              conditions:
                description: conditions represent the current state of the HermesAgent
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dashboardEndpoint:
                description: DashboardEndpoint is the HTTP endpoint where the Hermes
                  dashboard can be accessed.
                type: string
              dashboardPort:
                description: DashboardPort is the port where the dashboard service
                  is exposed.
                type: integer
              gatewayEndpoint:
                description: GatewayEndpoint is the HTTP endpoint where the Hermes
                  gateway can be accessed.
                type: string
              gatewayPort:
                description: GatewayPort is the port where the gateway service is
                  exposed.
                type: integer
              phase:
                description: Phase is the current phase of the Hermes agent.
                type: string
              podIP:
                description: PodIP is the IP address of the Hermes agent Pod.
                type: string
              podName:
                description: PodName is the name of the Hermes agent Pod.
                type: string
              serviceName:
                description: ServiceName is the name of the Service exposing the Hermes
                  agent.
                type: string
              startedAt:
                description: StartedAt is the time when the Hermes agent was started.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
