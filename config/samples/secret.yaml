# Secret for HermesAgent API key
# Create with: kubectl apply -f secret.yaml
# Usage: Reference the api-key value in HermesAgent env section
apiVersion: v1
kind: Secret
metadata:
  name: kimi-api-key
type: Opaque
stringData:
  api-key: "your-api-key-here"
