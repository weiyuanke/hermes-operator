# Exclude everything by default, then re-include only what the build needs.
**

# Go module files
!go.mod
!go.sum

# Source directories (must include the directory itself, not just *.go)
!cmd/
!cmd/**
!api/
!api/**
!internal/
!internal/**
!hack/
!hack/**
